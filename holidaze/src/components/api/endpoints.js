export const API_BASE_URL = "https://api.noroff.dev/api/v1/holidaze"

export const venuesUrl = `${API_BASE_URL}/venues`;

export const bookingsUrl = `${API_BASE_URL}/bookings`;

export const profilesUrl = `${API_BASE_URL}/profiles`;

export const registerUrl = `${API_BASE_URL}/auth/register`;

export const loginUrl = `${API_BASE_URL}/auth/login`;